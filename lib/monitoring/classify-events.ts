/**
 * @deprecated Import from `@/lib/classification` instead.
 * Thin re-export so monitoring/persist paths keep working during rescue.
 */
export {
  classifyNormalizedResult,
  DeterministicCitationClassifier,
  findBrandMatches,
  type ClassificationContext,
} from "@/lib/classification";
